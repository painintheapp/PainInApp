import React from 'react';

import ItemDetailStyles from './ItemDetailStyles';

const ItemDetail = ({
  backColor,
  leftType,
  leftImage,
  leftText,
  rightType,
  rightImage,
  rightText,
  HeaderText,
}) => <ItemDetailStyles.WrapperView />;

export default ItemDetail;
